{
  "name": "server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "sass": "sass --watch public/sass:public/stylesheets --poll",
    "nodemon": "nodemon app.js -L",
    "dev": "run-p sass nodemon",
    "start": "cross-env NODE_ENV=production node ./app"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "dotenv": "^8.2.0",
    "ejs": "^3.1.5",
    "express": "^4.17.1",
    "pg": "^8.5.1"
  },
  "devDependencies": {
    "morgan": "^1.10.0",
    "nodemon": "^2.0.7",
    "npm-run-all": "^4.1.5",
    "sass": "^1.32.7"
  }
}
